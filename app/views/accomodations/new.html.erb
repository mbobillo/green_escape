<div class="container">
<h1 class="m-2 p-5">Create a new accomodation</h1>
<%= simple_form_for @accomodation do |f| %>
    <div class="container p-5 m-2">
      <h4>Question 1</h4>
      <h3>Which of the following best describes your accommodation?</h3>
        <div class="type d-flex">
      <% [['Yurt', '0', 'fa-campground'], ['Cabin', '1', 'fa-home'], ['Tiny House', '2', 'fa-house-user'], ['Container', '3', 'fa-shipping-fast']].each do |label, value, icon_class| %>
      <div class="radio-item">
        <%= f.radio_button :category, value, id: "category_#{value}", class: "hidden-radio" %>
        <label for="category_<%= value %>">
          <i class="fas <%= icon_class %>"></i>
          <%= label %>
        </label>
      </div>
    <% end %>
  </div>
    </div>

    <div class="container p-5 m-2">
      <h4>Question 2</h4>
      <h3>Where is your accomodation? *</h3>
      <%= f.input :localisation, label: false,
      input_html: {data: {address_autocomplete_target: "localisation"}, class: "d-none"},
      wrapper_html: {data: {controller: "address-autocomplete", address_autocomplete_api_key_value: ENV["MAPBOX_API_KEY"]}}%>
    </div>
  <div class="d-flex">
      <div class="container p-5 m-2">
        <h4>Question 3</h4>
        <h3>What's the price for one night? *</h3>
        <%= f.input :price, label: false %>
      </div>
      <div class="container p-5 m-2">
        <h4>Question 6</h4>
        <h3>What's the capacity of your accomodation? *</h3>
        <%= f.input :capacity, label: false %>
      </div>
  </div>

    <div class="container p-5 m-2">
      <h4>Question 4</h4>
      <h3>Give your accomodation a title *</h3>
      <%= f.input :title, label: false%>
    </div>

    <div class="container p-5 m-2">
      <h4>Question 5</h4>
      <h3>Add a description</h3>
      <%= f.input :description, label: false %>
    </div>


    <div class="container p-5 m-2">
      <h4>Question 7</h4>
      <h3>Which is the type of environment for your accomodation?</h3>
      <%= f.input :environment_tag, as: :radio_buttons, collection: [['Mountain', '0'], ['Coast', '1'], ['Countryside', '2'], ['Urban', '3']], legend_tag: false %>
    </div>

    <div class="container p-5 m-2">
      <h3>Add an image</h3>
      <%= f.input :photos, as: :file, label: false, input_html: { multiple: true } %>
    </div>

    <div class="container p-5 m-2">
      <%= f.submit "Submit", class: "btn btn-primary" %>
    </div>
<% end %>
</div>
